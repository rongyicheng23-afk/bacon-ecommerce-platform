# Bacon Mall 后端环境变量
# 复制此文件为 .env 后修改

# 数据库路径（默认项目根目录下的 bacon_mall.db）
BACON_DB_PATH=

# CORS 允许的前端地址（逗号分隔）
BACON_CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# MinIO 对象存储（可选，不配置则使用外部 URL）
# MINIO_ENDPOINT=127.0.0.1:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
